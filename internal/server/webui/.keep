# 目录占位：Web UI 是单文件 index.html，页面本体见同目录 index.html。
#
# 这个文件的作用不是给页面用，而是让 //go:embed all:webui 在 index.html 被删掉后**仍然编译通过**：
# 页面缺失由 handleIndex 在运行时回 500（并记一条告警），回归用例因此能把它判成断言红，
# 而不是把「页面不在二进制里」变成一个构建错误。
