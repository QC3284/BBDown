# 变更日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

**版本号语义**：`<上游版本>-go`。例如 `1.6.19-go` 表示「行为已对齐到上游 C# v1.6.19」，
版本号本身就是对齐进度声明，而不是独立的产品版本线。

上游（aliveranme/BBDown）的同名版本条目仍是行为的权威描述；本文件只记录 Go 重写侧
**相对上游的落地情况**。逐条对账基线与判定见 [docs/UPSTREAM_ALIGNMENT.md](docs/UPSTREAM_ALIGNMENT.md)。

## [1.6.19-go.10] - 2026-09-19

补丁版本：本轮差分撞出并补齐**整条缺失的数据链**——背景音频与角色配音。

### 新增

- **解析 `dubbing_info` 并下载配音轨**：上游 `Parser.cs` 会读 `data.dubbing_info` 的
  `background_audio` 与 `role_audio_list`（门控为 APP API + 番剧），本仓此前**完全没有这段解析**
  ——两个列表永远为空，工作流里「背景音轨下载」「配音下载」两段循环等同于死代码，有配音的稿件
  产物里会缺背景音轨与全部配音轨。现补齐：解析（含 `backup_url` 选择、带宽 /1000、`audio_id`
  净化后按上游规则拼 `<aid>/<aid>.<cid>.<seg>.m4a`）、下载、混流物料。
- 配音的下标按每个 role 自己的音频列表夹取（上游 `ClampRoleAudioIndex`：越界钳末位、空列表跳过），
  `entity.AudioMaterialInfo` 增加 `AudioID` 承载该 role 的 `audio_id`。

### 说明

- 本轮同时接入 `BBDownLoginUtilMergeTests`（新版协议 Set-Cookie 合并）与 `WidevineCryptoTests`
  （AES-CMAC 的 RFC 4493 已知答案向量、子密钥 L、PKCS7 往返与畸形填充）。
- 上游 `BBDown.Tests` 61 个测试文件至此全部定性：接入差分 40 个、同契约已覆盖 6 个、
  不适用 7 个（C# `AsyncLocal`/AOT 反射/程序集元数据/假服务器夹具等），其余为夹具与元数据。
## [1.6.19-go.9] - 2026-09-19

补丁版本：差分续跑（目标轮 5~8）撞出的六处不一致，含两处「整块能力缺失」。

### 新增

- **风控页识别**：接口以 HTTP 200 返回 HTML（风控页/登录墙）时给出「疑似风控页」可读诊断，
  不再把裸 JSON 语法错误抛给用户；parser/fetcher/评论导出的 22 处解析统一走 `util.UnmarshalJSON`。

### 修复

- **播放受限不给原因**：按 `limit_play_reason` 映射为区域/付费/大会员/未到时间等说明。
- **官方域名白名单少 `biliapi.com`**：会导致凭据校验误判「非可信主机」拒绝发 Cookie、重定向守卫误拦。
- **取消归类过宽**：`ctx.Err() != nil` 把超时也算成用户取消，任务被误标「已取消」、真实失败被掩盖；
  现只认 `context.Canceled`，超时归 Failed 并保留原始错误，真取消统一文案「已取消」。
- **gRPC 空载荷被当成畸形帧**：flag=0 且长度为 0 的帧是合法响应，应返回空数组。
- **无 magic 的 v2 `.wvd` 被误判为无法识别**：探测只放行了首字节 1。

### 说明

- 差分用例累计覆盖上游测试文件 37 个（`internal/*/upstream_*_test.go`）。
- 许可证链路的两条安全前提（禁重定向、始终校验证书）本轮补上钉住用例——此前零守卫。
## [1.6.19-go.8] - 2026-09-19

补丁版本：差分续跑撞出两处（一处整块缺失、一处解析口径），另确认一处不适用。

### 新增

- **风控页识别**（对齐上游 `RiskControlResponseException`）：B 站的风控页/登录墙/错误页都以
  HTTP 200 + HTML 返回，本仓此前把 HTML 直接交给 `json.Unmarshal`，用户看到的是
  `invalid character '<' looking for beginning of value`——既难定位又像偶发。
  现由 `util.LooksLikeHTMLPage`（剥前导空白与 BOM 后判首字符）识别，
  经 `util.UnmarshalJSON` 给出「疑似风控页：接口返回 HTML 而非预期数据…」的可读诊断；
  parser / fetcher / 评论导出的 22 处解析统一走它。

### 修复

- **JSON 数字取值容忍前缀**：`gi`/`gi64` 用 `fmt.Sscanf("%d")`，`"12abc"` 会被当成 12、
  `"1.5"` 当成 1；上游 `int.TryParse(NumberStyles.Integer)` 要求整串是数字，脏数据落到默认值 0。
  现改为整串解析（`strconv`）+ int32 范围判定。

### 说明

- 重试策略（5xx 重试、4xx 不重试、耗尽即停、请求计数精确、响应体上限）与上游一致，
  既有 `retry_test.go` 已按同一口径断言。
- 上游 `ConfigPropagationTests` 用反射检查 C# 的 `AsyncLocal` 传播，Go 无对应机制：
  本仓等价契约由返回值承担（`InitSession` 返回 wbi 并回写 cfg，调用点传给 factory），
  不存在「子方法写完父流程看不见」的形态，已记入对齐文档。
## [1.6.19-go.7] - 2026-09-19

补丁版本：继续逐文件搬上游测试表。本轮接 12 个文件，撞出四处不一致并修复；
其余八处跑下来确认与上游一致（用例已钉住）。

### 修复

- **媒体下载的 UA 写死成 `Mozilla/5.0`**（`HttpUtilUserAgentTests`）：上游的优先级是
  显式参数 → 流配置（`--user-agent`）→ 进程级随机默认，而本仓下载路径（探针 HEAD / 单线程
  GET / 多线程 Range / aria2c 输入文件）一律写死裸 `Mozilla/5.0`——用户设了 `--user-agent`
  只影响 API 请求，媒体下载仍带着这个极易被 CDN 识别的 UA。现统一走 `DownloadConfig.userAgent()`。
- **字幕时间轴的毫秒取整**（`SubtitleFormatTests`）：本仓用「小数部分 ×1000 截断」，
  `1.001` 秒的差值算出来是 `0.000999…`，截断得 0ms，与上游 `TimeSpan.FromSeconds` 的
  tick 级四舍五入差 1ms。改为按 tick 进位后再截断到毫秒。
- **字幕时间轴的 NaN 与极大值**：`int(NaN)` 在 Go 里是实现相关行为（会算出垃圾时间），
  上游把 NaN 与负数一律归 0；极大值也先夹到 `TimeSpan.MaxValue` 量级再格式化。
- **`SanitizeSRT` 只裁 ASCII 空格/制表符**：上游用 `TrimEnd()`（Unicode 空白，含全角空格）。

### 说明

- 本轮确认无差异（已加用例钉住）：弹幕关键词/midHash 过滤、ASS 大括号与反斜杠中和、
  弹幕色 BGR 转换与白色省略、时间轴点号分隔、充电试看判定、SESSDATA 到期估算、
  服务器时钟校准、专栏 cv 号解析与 Markdown 头部、`--work-dir` 路径解析。
- 逐跳重定向校验与 Logger 文件行为的上游用例依赖各自的本地服务/句柄夹具，
  本仓对应用例（`credhost_test.go`、`loggerfail_test.go`）已覆盖同一契约。
## [1.6.19-go.6] - 2026-09-19

补丁版本：继续搬上游测试表做差分，撞出六处不一致（其中两处是安全/数据完整性）。

### 修复

- **日志脱敏漏掉签名媒体 URL 的临时授权参数**（安全）：上游 `SensitiveDataMasker` 把
  `sign` / `x_sign` / `w_rid` / `deadline` / `marlin_token` 一并脱敏——这些是 CDN 下载票据与
  DRM 许可令牌，明文落进日志文件就等于把「可用的临时下载权（含付费内容）」写在了盘上。
  本仓只掩账号类凭据，现补齐同一张键表。
- **脱敏会改写 URL 本身**：本仓用 `net/url` 往返一圈，掩码里的 `***` 被编码成 `%2A%2A%2A`，
  其它参数也会被重新编码；上游是对 query 做字符串手术、其余部分逐字保留。现照做。
- **误掩非凭据**：`buvid3` 是设备标识不是凭据，上游保留原文便于排查，本仓此前把它掩掉。
  URL 与 Cookie 现共用上游那张敏感键表。
- **分片合并失败会留下半截产物**（数据完整性）：上游在失败/取消时删除输出文件，
  本仓此前把半截文件留在最终路径上——后续的「已存在」判定可能把它当成完整产物。
- **分片合并缺四件事**：单分片应直接 rename（上游如此，省一次整段拷贝）、输出目录不存在
  自动创建、支持取消（中途取消不留下空/半截文件）、空列表不产出文件。现逐条对齐，
  函数签名加上 `ctx`（与上游带 CancellationToken 一致）。

### 说明

- 新增差分用例：`SensitiveDataMaskerTests`（12 条）、`CombineFilesTests`（5 条）、
  `NumericOptionValidationTests`（11 条，跑下来确认本仓的数值校验范围与文案已逐字一致）。
- `TestMergeFLVCleansIntermediatesOnFailure` 原本靠「输出目录不存在」制造合并失败——
  自动建目录之后这条不再成立，改用「父路径是普通文件」制造失败，断言的性质不变。
## [1.6.19-go.5] - 2026-09-19

补丁版本：把上游测试的数据表整表搬来做差分，撞出六处行为不一致。

### 修复

- **`article` / `live` 的 `-w`**：上游在两个子命令的 settings 里各自声明 `-w|--work-dir`
  （根命令的 `--work-dir` 无简写）；本仓只注册了长选项，`BBDown article -w <dir>` 直接报
  unknown shorthand flag。
- **分P范围两侧的空白**：`-p "1 - 3"` 在上游合法（`int.TryParse` 容忍两侧空白），
  本仓用 `Sscanf("%d-%d")` 解析会报「无法识别的分P范围」。
- **识别不出的输入**：上游抛「输入有误：无法识别的视频 URL 或 ID」；本仓原样透传，
  非法输入带着毫无线索的字符串流向解析层。
- **`FormatFileSize`**：上游 <1KB 输出「{n} bytes」且**没有 TB 档**；本仓输出「0.00 B」并多一档，
  同一体积的显示与上游不同。
- **文件名截断**：上游超长时保留扩展名、长度恰为上限、**不加省略号**；本仓加省略号并把 `.mp4`
  一起截掉（混流产物会因此丢掉类型标识）。
- **清晰度 id 解析**：上游按 Int32 解析、畸形或超范围一律降级 0；本仓用 64 位解析，
  服务器下发的 `999999999999` 会被当成合法值参与排序，轨道顺序与上游不同。

### 说明

- 六条都由「把上游测试的数据表搬来做差分」撞出来：PageSelection / FormatHelper / PathUtil /
  BvConverter / TrackSort / UrlResolver / PathFormat 七个上游测试文件的表已接入本仓用例。
- `GetValidFileName("")` 返回空串是本仓既有契约（调用方以空串表示「无值」），与上游的 `"_"`
  不同，已在对齐文档中显式记录为已知差异。
## [1.6.19-go.4] - 2026-09-19

补丁版本：按「上游可见消息 vs 本仓」逐条比对，补齐七处未对齐行为。
起因是「还有哪些与上游不一致」——把上游 75 个源文件按「文档是否提及」过一遍筛出未审计文件，
再抽取上游 282 条 Logger 调用的字面量逐条比对，发现五片审计报告都没覆盖到的显示层与分P选择。

### 修复

- **稿件头部**（`Workflow.cs:142-153`）：`视频标题:` / `视频URL:` / `UP主页:` 三个标签此前缺失
  （标题与 URL 都是裸值、UP 主页根本不打印），发布时间也不带时区偏移。
- **互动视频 + `-t`**（`Workflow.cs:156`）：上游发现互动视频不支持 TV 端下载时会就地改回默认
  解析并提示；此前 `IsSteinGate` 解析出来却没有消费者，`-t` 会照着 TV 接口一路失败。
- **`--only-show-info` 的取流地址**（`Display.cs:49,60`）：参数此前被忽略，`-I` 只给出体积/码率
  清单，脚本拿不到 `baseUrl`。
- **背景音频与配音清单**（`Display.cs:19-35`）：两者都存在时上游会先打印「共计N条背景音频流」
  与「共计M条配音, 每条包含K条配音流」，本仓只列视频流与音频流。
- **分P别名的静默错页**（`Pages.cs:ExpandPageAliases`）：`-p LAST/NEW/LATEST` 此前用子串替换
  展开，`-p 1LATEST`（拼错）会被悄悄改成页码 112 并照此下载；上游按逗号分段、整段全词匹配，
  非别名段原样交给解析器报错。
- **自动选集数的提示**（`Pages.cs:22-33`）：按 epid 或 URL 的 `p` 参数自动选集数时上游会明确
  告知；另外 `p` 参数此前只认 `?p=` 开头，`?a=1&p=4` 会被漏掉。
- **零产物假成功**（`Download.cs:553-563`）：dash 分支没有视频流/音频流时要打印「没有找到符合
  要求的视频流/音频流」，且 `--video-only`/`--audio-only` 必须判失败；此前只清空轨道，
  用户拿到零产物却看到「任务完成」。

### 说明

- 每条修复都带回归用例，并逐一做变异验证（撤掉修复即变红）。
- 扫描方法记录在 `docs/UPSTREAM_ALIGNMENT.md` §4.18，可直接复用于下一次对齐。
## [1.6.19-go.3] - 2026-09-19

补丁版本：修掉「一次下载出现两个进度」的终端渲染问题，并把多线程进度聚合对齐上游。

### 修复

- **进度条与日志互相踩行**：进度条是原地重绘的，日志又直接写在当前光标处，两者没有共同锁、
  且下载结束打「合并分片...」时不等进度行收尾——于是上一帧的残影（实测 25.07%）被日志压掉
  左半边，紧接着又画出最后一帧，看起来就是「两个进度」。
  现按上游 `Logger.ConsoleLock` 让日志与进度条共用一把锁，日志写入前先把进度行收尾（换行），
  收尾后再打日志（与上游 `using var progress` 的 `Dispose` 时序一致）。
- **进度条收尾不擦除**：上游 `ProgressBar.Dispose` 走 `UpdateText(string.Empty)` 把整行擦掉；
  本实现此前把最后一帧留在屏幕上，正是它让紧随其后的日志与进度条挤在同一行。现同样擦除。
- **多线程进度按分片完成跳变**：此前只在分片**下载完**才累加字节，默认分片数下进度以 25% 为
  台阶跳变（0 → 20.90% → 52.54% → 100%）。移植上游 `ProgressAggregator`：分片上报各自的
  **累计**字节，聚合按「新值 - 上次值」推进，重试回退时总量随之回退（不越过 100%）。
  实测同一命令由 0/20.90/52.54/100 变为 0/34.99/63.41 的连续推进。

### 变更

- 单线程进度条同样不再把最后一帧留在屏幕上（上游语义）；快速下载会看到进度条出现后消失。

## [1.6.19-go.2] - 2026-09-19

补丁版本：三处与上游可见行为不一致的修正——终端配色、登录二维码极性、布尔选项的上游写法。

### 修复

- **终端配色（对齐上游 .NET ConsoleColor）**：ConsoleColor 的 Red/Cyan/White 是**亮色**变体，
  本实现写成了 31/36/37 这组暗色 SGR（横幅前景只给了 37），错误行、提示行与横幅都比上游暗。
  现统一映射到 9x 亮色系（AnsiRed=91、AnsiCyan=96、AnsiWhite=97），DarkYellow=33、
  DarkGray=90、DarkBlue 背景=44 保持原值。
- **登录二维码极性**：上游 `ConsoleQRCode.GetGraphic` 把模块矩阵里为 true 的深色模块画成
  ConsoleColor.Black；本实现画反了（深色模块用白底 47、浅色模块用黑底 40），整张码是反色的
  ——深色终端上多数扫码器仍认，浅色主题终端会扫不出来。
- **布尔选项的上游写法**：上游（System.CommandLine）布尔选项 arity 为 0..1，上游 README 就按 `--multi-thread false`
  文档化「关闭多线程」；而 pflag 只认
  `--flag=false`，于是 `false` 被当成输入地址去请求 API，用户看到的是
  `获取视频信息失败 (code=-400): 请求错误`——既关不掉多线程，报错也指向错误的原因。
  现于参数归一化层折平长/短别名，大小写不敏感（对应上游 Boolean.Parse），
  `--` 之后不再折行，配置文件里的同一写法一并折行。

### 变更

- README 的 `--multi-thread` 一行补上 `--multi-thread false` 的关闭写法（与上游一致）。
- 修正本文件与 docs/ 下共 173 处被误写成「反斜杠+反引号」的行内代码标记（例如把
  `GetValidFileName` 写成 `\`GetValidFileName\``）——那些反斜杠会在 GitHub 上原样显示。

## [1.6.19-go.1] - 2026-09-19

补丁版本：修正 `1.6.19-go` 发布后发现的问题。**该版本的二进制来自一个 CI 失败的提交**，
故不移动已发布的 tag，而是以补丁版本重新出包。

### 修复

- **文件名净化契约**：`GetValidFileName("")` 曾返回占位符，而调用方用空串表示「无值」；
  尾随点/空格裁剪同步更新既有测试的期望（对齐上游 v1.6.18）。
- **Windows 路径语义**：`filepath.IsAbs("/abs/x")` 在 Windows 为 false，根相对路径被错误并入
  `--work-dir`；改用 `isRootedPath`（对齐上游 `Path.IsPathRooted`）。
- **直播产物完整性**：录制结果三态化（区分「未录到内容」与「合成失败但分段可恢复」）；
  合成改为写 `<path>.staging` 并在通过大小校验后改名，不再留下看似完成的截断产物；
  此前保留的旧会话目录**只提示不删**。
- **并发安全**：目标路径独占锁，锁覆盖「存在性检查 → 下载 → 混流」，避免并发任务互相覆盖产物。
- **`--aria2c-args` 引号感知切分**：`strings.Fields` 会把带空格的引号值拆成多个参数，
  未闭合引号还会吞掉整段配置。
- **`article`/`live` 支持 `--work-dir`**：此前忽略该选项，产物落进程目录；文件名接入保留名防护。
- **登录有效期**：`SESSDATA` 剩余 ≤7 天时告警、已过期时提示重新登录（解析 fail-open，不误报）。
- **日志写失败退避**：连续 5 次写失败后暂停文件日志 30 秒，避免磁盘满/只读时每行重试一次的失败 open。
- **UA 版本池**前移到 130-150（过旧的版本本身就是指纹）。

### 工程

- 新增 `.github/workflows/release.yml`：推送 `v*` tag 即自动跑测试、构建 5 平台并挂载产物。
- `AGENTS.md` 补充测试纪律：推送前必须 `go test ./... -count=1`（默认会命中测试缓存）、
  不要用固定 `time.Sleep` 等待异步副作用、断言失败前先释放资源。

## [1.6.19-go] - 2026-09-19

首个完成全面对账的版本：以 C# 上游 v1.6.19 为目标，逐条审计 187 项行为变更
（✅ 50 / ⚠️ 35 / ❌ 84 / ➖ 17 / ❓ 1），关闭全部 7 条 P0，并移植上游 15 个解析夹具作为回归网。

### 修复 —— 正确性与数据完整性

- **混流必败**：`-metadata:s:*` / `-disposition` / `-map_chapters` 曾紧跟各自的 `-i`，
  被 ffmpeg 当成下一个输入文件的选项。带封面+章节、≥2 字幕或任何背景音轨时混流以
  `exit 234`（"cannot be applied to input url"）失败（对齐上游 v1.6.16）。
- **mp4box 丢弃配音/背景轨**：杜比视界自动切换 mp4box 时这些轨道被静默丢弃，随后被清理删除；
  现进入 `-add` 链并以 `-udta` 命名（对齐上游 v1.6.17）。
- **直播录制删除已录内容**：`defer os.RemoveAll(segRoot)` 会连历次保留的分段一起删，
  而三处日志却称"已保留"；读中断还会先删分段再判断取消，Ctrl+C 丢整段。
- **直播录制挂死**：无读停滞看门狗，重连上限 3 次。现 60s 看门狗 + 不限次指数退避（3s→30s），
  本地写盘失败归为终结态不再重试。
- **直播分段尾裁剪**：网络中断留下的半截 FLV 标签会让 concat demuxer 中止整场合成。
- **合并产物大小校验**：ffmpeg 遇坏段会截断并仍以 0 退出，紧随其后的分段清理会静默丢掉整场录制。
- **入档粒度**：多P稿件的所有分P共享同一 aid，原先下完第一个分P就写入 `BBDown.archives`，
  下次运行余下分P被判定已下载而跳过（对齐上游 RF-75 `ArchiveTracker`）。
- **CoverOnly 仍下载完整视频**：代码注释误称"matching C#: no early return"，上游实际明确提前返回。
- **混流非事务化**：直写 `savePath` 时中断会留下截断成品，并被"已存在, 跳过下载"永久当成已完成。
- **免二压重发协议整体缺失**：现 qn=0 首请求 + qn=127 重取，只有带 `dash.video` 的文档才接管，
  重取被拒时沿用首轮已验证响应，用户取消向上传播。
- **VOD 读停滞**：媒体下载用无总超时的 client，网络黑洞下 `io.Copy` 永久阻塞。
- **文件名卫生**：尾随点/空格被 Windows 静默丢弃；基名无长度上限。

### 修复 —— 安全

- **serve 门禁整片缺失**：未配 token 时中间件根本不挂——不是"校验不严"而是"没有校验"。
  现无条件安装 Host 校验（DNS rebinding）与 Origin/Content-Type 校验（CORS 简单请求 CSRF）。
- **路径穿越**：`<aid>` / `<cid>` / `<dfn>` / `<res>` / `<fps>` / `<videoCodecs>` /
  `<audioCodecs>` 与字幕 `lan` 裸替换；新增 `SanitizePathSegment` 并接入 13 处。
- **携凭据请求可被 3xx 引走**：跨主机重定向一律拒绝（登录轮询 / gRPC POST / TV 登录）。
- **畸形响应 panic**：零字节与截断的 `device.wvd`、垃圾 protobuf（长度溢出）、DRM 许可证解析
  无边界检查都会崩进程，而全仓 `recover()` 0 命中。
- **解析死循环**：未知 wire type 时游标不推进，同一批畸形输入即可挂死。
- **日志注入**：服务器可控文本（标题 / UP 名 / 接口 message）可含 CRLF 伪造日志行或 ANSI 转义。
- **serve token**：明文比较改为常量时间；新增 `BBDOWN_SERVE_TOKEN` 环境变量优先级。
- **认证失败限速**：token 原本可被无限次尝试。
- **新增 `--trusted-proxy`**：`X-Forwarded-For` 仅在请求确实来自配置的代理时采信。
- **aria2c 输入注入**：URL 直接来自接口 `base_url`，可带换行注入 `out=` / `dir=` 选项。
- **`/get-tasks` 查询并发限流**；超大请求体返回 413 而非通用 400。
- **DRM 取钥链**：贯通 context（取钥窗口最长约 6 分钟完全不可取消）、许可证请求改禁跳转 +
  64MB 有界读取，调用点包 2 分钟超时。
- **任务持久化**：唯一临时名 + 互斥串行写盘；内存已完成列表按上限裁剪；错误信息脱敏本机绝对路径。
- **serve 关停**：原先只等待不取消，在途任务既不落 Cancelled 也不落盘，30s 后被进程退出截断。

### 修复 —— 行为一致性

- `--host` 在 UGC playurl 路径上失效（硬编码 `api.bilibili.com`）。
- 配置合并的 URL 启发式把选项值（`--aria2c-proxy http://…`、`--work-dir av123`）当成目标 URL，
  压掉 `BBDown.config` 里的真实地址并报"缺少参数"。
- 指定 ep 未命中时返回空 Index，workflow 回落 ALL —— 请求单集却静默下载整季。
- `sub check` 全部视频失败仍以退出码 0 结束。
- SubOnly 无条件把字幕改成 `.srt`（ASS/JSON 轨道扩展名与内容不符）。
- 评论导出不建父目录；直播录制前不加载凭据（永远游客画质）；登录轮询顶层 `code` 未校验；
  空 `access_token` 会被写盘。
- 直播画质从固定的游客档 qn=10000 提升到 qn=30000 并按账号权限回落。
- DOVI 探针无超时；补 `WaitDelay`（否则探针 fork 出的子进程持有管道时仍会阻塞）。

### 测试

- 移植上游 15 个 `Fixtures/parser/*.json` 夹具与 `httptest` 回放基座（含按 query 分流的多轮响应，
  用于免二压重发与 INTL 双次请求）。
- 用例规模：19 个测试文件（无 `httptest`）→ **126 例通过**；`internal/muxer`、
  `internal/parser`、`internal/drm` 三个包从零测试到有回归网。
- 每条修复都以「**撤掉修复必须让测试变红**」验证（变异验证），避免留下假绿的回归网。

### 文档

- 新增 [docs/UPSTREAM_ALIGNMENT.md](docs/UPSTREAM_ALIGNMENT.md)：参照源、项目谱系、可复现的对账步骤、
  判定总表、P0/P1 队列、逐轮修复记录与剩余队列。
- 新增 [docs/alignment/](docs/alignment/)：五片审计明细，逐条附 Go 侧 `file:line` 证据。
